package cpen221.soundwaves.soundutils;

/**
 * <p><strong>Overview.</strong>
 * The {@code FilterType} enum provides the types of filtering
 * operations that are permitted on the {@code SoundWave} type.
 * </p>
 *
 */
public enum FilterType {
    LOWPASS,
    HIGHPASS,
    BANDPASS;
}
